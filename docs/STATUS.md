# STATUS — текущее состояние проекта (Юр-бот «URIST2026»)

> **Для Claude и человека**: это первый файл, который читается в новой сессии. После
> прочтения **обязательно** сверить с реальностью: `git tag -l "stage-*"`,
> `git log --oneline -20` — таблица ниже может быть устаревшей.

**Последнее обновление**: 2026-07-03 (закрыт Этап 8 — помощь + анти-абьюз, tag stage-8-done)
**Текущий этап**: Этап 9 — Аналитический слой БД (сбор без UI) (☐ не начат)
**Следующий шаг**: Взять Этап 9 в работу. Критерии — [`07_ROADMAP.md`](07_ROADMAP.md#этап-9--аналитический-слой-бд-сбор-без-ui). Задел уже есть: `user_events` (журнал действий, этап 0), `users.last_active`/`is_paying`, `payments`, `search_usage`. Уточнить, каких событий не хватает для конверсии (регистрации/запросы/поиски/платежи).

---

## Сводка этапов

Легенда: ☐ не начат · 🚧 в работе · ✅ закрыт · ⏸ заблокирован

| # | Этап | Статус | Закрывающий тег | Последний коммит | Дата закрытия |
|---|---|---|---|---|---|
| 0 | Каркас репозитория | ✅ | `stage-0-done` | `80506b3` | 2026-07-02 |
| 1 | Онбординг + меню + БД юзера | ✅ | `stage-1-done` | `66ebcc3` | 2026-07-02 |
| 2 | Базовый ИИ-ответ + память + списание | ✅ | `stage-2-done` | — | 2026-07-02 |
| 3 | ИИ-агент с веб-поиском (Tavily) | ✅ | `stage-3-done` | — | 2026-07-02 |
| 4 | Адаптивный сбор ситуации (FSM) | ✅ | `stage-4-done` | `8f7c913` | 2026-07-03 |
| 5 | «Проверить работодателя» | ✅ | `stage-5-done` | `41116e8` | 2026-07-03 |
| 6 | Оплата ЮKassa (пакеты 10/20/30) | ✅ | `stage-6-done` | `658428c` | 2026-07-03 |
| 7 | Админка `/admin` | ✅ | `stage-7-done` | `abc7129` | 2026-07-03 |
| 8 | Помощь + дисклеймеры + анти-абьюз | ✅ | `stage-8-done` | `972a582` | 2026-07-03 |
| 9 | Аналитический слой БД | ☐ | `stage-9-done` | — | — |
| 10 | Прод: деплой + CI/CD | ☐ | `stage-10-done` | — | — |

Детальные критерии приёмки каждого этапа — в [`07_ROADMAP.md`](07_ROADMAP.md).

## Активная работа

Пока нет. Этап 8 закрыт, ждём отмашку на старт Этапа 9.

## Известные блокеры

Нет.

## История закрытий

2026-07-03 — Этап 8: Помощь + дисклеймеры + анти-абьюз — tag stage-8-done — справка для аудитории 35–50 и защита баланса владельца от слива. **Анти-абьюз** (config: `max_message_length=1000`, `rate_limit_messages=5`/`rate_limit_window_seconds=10`, `collect_dialog_ttl_seconds=900`): `AntiAbuseMiddleware` (`middlewares.py`) режет флуд и слишком длинные сообщения ДО хендлеров — rate-limit на Redis-счётчике (`urist:rl:{uid}`, INCR + EXPIRE окна), предупреждение ровно один раз за окно (при `count == limit+1`), дальше молча гасит; длина > лимита → понятный текст, в модель не уходит. Команды (`/start`/`/admin`/`/id`) не блокируются (запасной выход); callback'и не режем (не жгут токены, не ломаем оплату/навигацию); сбой Redis не роняет обработку (best-effort, пропускаем без лимита). Регистрируется в `main.py` ПОСЛЕ `LoggingMiddleware` — заблокированное всё равно попадает в лог/`user_events`. **Таймаут брошенного сбора** (`handlers/ask.py`): в состоянии COLLECTING храним `collect_updated_at`; если юзер вернулся позже TTL — старый контекст не тащим и не оплачиваем, `_collect_step` сбрасывает `case`/историю и начинает заново (§10 таймаут; экономия токенов flash-lite на разросшейся истории; списание и так только в единственной точке `_finalize_answer`, брошенный диалог её не достигает). Тексты `too_long()`/`RATE_LIMITED` (оба «запрос не списан»). **Помощь** (`texts.screen_help`): добавлен раздел «Как работает»; явно — уточнения/отмена/таймаут/«не по теме» не списываются; оплата через ЮKassa; поддержка по работе и оплате. Контакт поддержки — плейсхолдер `@URIST2026_support` (реального заказчик пока не дал — открытый вопрос владельца, тянется с этапа 1). Переиспользовано без изменений: лимит уточнений (`max_collect_steps`), обрезка памяти (`dialog_memory_messages=10`), капы поиска/токенов. Проверка машинно (bot-контейнер, живой Redis/FSM): rate-limit 5/10с — первые 5 проходят, 6-е+ гасятся, предупреждение ровно одно; таймаут сбора — детерминированный прогон `_collect_step` с состаренной меткой (>900с) через реальный `RedisStorage`: лог «Сбор устарел → начинаю заново», финал ушёл с НОВЫМ вопросом, старый `case` (Москва/«не платят зарплату») не протёк. Живая проверка человеком (@Polarasing): «вроде ок» — п.1 помощь, п.2 длинное сообщение, п.3 флуд подтверждены; п.4 таймаут не тестил живьём (долго ждать) — закрыт машинным тестом. Repo без remote — коммиты только локально в `dev`.

2026-07-03 — Этап 7: Админка `/admin` — tag stage-7-done — управление ценами и ключами поиска прямо из бота, всё горячо (без рестарта). Доступ только `settings.admin_id_list` (`ADMIN_IDS` в `.env`; для теста выставлен `2042819654` @Polarasing); отказ и на команду, и на callback'и (`_guard_cb`). UI — одно перерисовываемое inline-сообщение (`handlers/admin.py`): шапка+меню → экран ввода значения (с «Отмена») → снова меню; FSM `AdminStates.waiting_value`, тип правки в `state.data["kind"]`. **Шапка** (`texts.admin_header`): всего/платящих/неплатящих юзеров (`repo.user_counts` — один SQL с `FILTER (WHERE is_paying)`); балансы сервисов — реальный остаток кредитов OpenRouter (`ai.account_credits` → `GET /credits`, остаток = total_credits − total_usage, ошибки не валят админку) + расход Tavily/Exa/Firecrawl **собственным счётчиком** (у провайдеров нет простого usage-API; таблица `search_usage` в migration `0003_admin`, инкремент `settings_repo.bump_search_usage` на каждый реальный вызов провайдера в `search.run_web_search`; best-effort, не роняет поиск); текущие цены и маскированные ключи (`····abcd`). **Горячая правка** (`app_settings`): цены пакетов 10/20/30 → `package_prices` на экране «Баланс и оплата»; цена запроса — `repo.charge_one` теперь списывает `price_per_request` (дефолт 1 ⇒ поведение этапов 2–6 идентично, guard `balance >= cost`, при нехватке `None`); ключи поиска — **переезд из `.env` в `app_settings`**: `settings_repo.search_key(pool, provider)` читает БД с фолбэком на `.env`, `search.run_web_search(query, pool)` и `ai.answer_with_search(..., pool)` протянуты (pool из хендлеров `ask`/`employer`) — новый ключ берётся со следующего поиска. Навигация без тупиков: у ввода «Отмена», у меню «Обновить»/«Закрыть», `/start` сбрасывает FSM; при неудаче `edit_message` меню отправляется новым сообщением. Валидация: цена — положит. Decimal (запятая→точка), цена запроса — целое ≥1, ключ — непустой. Машинная проверка (bot-контейнер, живой пул): миграция 0003 применена, `search_usage` засеян; горячая правка цены (199→250→restore) и ключа (.env `····KOzx` → override → снова .env); `bump` счётчика; `charge_one` при cost=2 списывает по 2 и даёт `None` при нехватке; OpenRouter `/credits` вернул реальный остаток $9.96; шапка сошлась с прямым SQL (1/1/0); `ADMIN_IDS` подхвачен контейнером `[2042819654]`. Живая проверка человеком (@Polarasing): «работает вроде» — `/admin` открывается, правки применяются. Прод-заметка: боевые ключи и `ADMIN_IDS` — в прод-`.env` (Этап 10); значения в `app_settings` переопределяют `.env`. Repo без remote — коммиты только локально в `dev`.

2026-07-03 — Этап 6: Оплата ЮKassa (пакеты 10/20/30) — tag stage-6-done — покупка пакетов запросов через ЮKassa без вебхука (домена нет). Миграция `0002_payments`: `app_settings` (key/value; сиды цен пакетов 199/349/499 ₽ + `price_per_request=1` — задел под /admin Этап 7) и `payments` (упрощённая, без подписок: `package`/`amount`/`status`/`credited`). `src/yookassa.py` — async-клиент REST v3 (Basic-auth, Idempotence-Key, опц. http/socks-прокси для RU-выхода на проде). `src/receipt.py` — чек 54-ФЗ. `src/settings_repo.py` — чтение/запись `app_settings`, цены пакетов. `src/payments.py` — `start_payment`/`sync_payment`/`poll_pending` (уведомление об успехе — единый источник, не дублируется). `repo.credit_payment` — идемпотентное зачисление (FOR UPDATE + флаг `credited`; `balance += package`, `is_paying=true`; повторный вызов не пополняет). `handlers/payment.py` — экран баланса + цены + история + inline-пакеты; buy → счёт (Оплатить URL + Проверить + Отмена); check → sync; cancel → отмена; `send_balance_screen` переиспользуют ветки вопроса/работодателя при balance=0 (тупик оплаты теперь с кнопками пакетов). `main.py` — фоновый поллер pending-платежей (APScheduler, `payment_poll_interval_min=1`); allowed-updates добавил `callback_query`. Тестовые ключи ЮKassa из club_bot (тестовый магазин 1219310). Проверка машинно: `start_payment` → реальный тестовый платёж + confirmation_url + строка payments; `credit_payment` идемпотентен (3× → +10 один раз, credited_now только у первого, is_paying=true). Живая проверка человеком (@Polarasing), все 4 критерия из лога/БД: buy:10 → тест-карта → «Пакет 10 зачислен, баланс 10» (payments succeeded/credited=t); поллер каждую минуту не задвоил (баланс остался 10); buy→cancel → payments canceled/credited=f, баланс не изменился; экран «Баланс и оплата» показывает «Ваши покупки». Прод-заметка: `YOOKASSA_PROXY` для RU-выхода на зарубежном сервере, боевые ключи — в прод-`.env` (Этап 10).

2026-07-03 — Этап 5: «Проверить работодателя» — tag stage-5-done — отдельная ветка-сценарий `src/handlers/employer.py` поверх агента поиска (Этап 3), роутер включён до `menu`. `EmployerStates.waiting_input`: `open_employer` (balance=0 → оплата), `handle_employer` — шаблонный промпт `texts.employer_query` (название/ИНН/ссылка → инструкция агенту: организация, отзывы, тревожные признаки, вывод; при распространённом названии — честно просит ИНН/ссылку) уходит в тот же `ai.answer_with_search` (память диалога НЕ используется — разовая проверка), сводка + `sources_block` + `EMPLOYER_DISCLAIMER` («данные из открытых источников») + `answer_footer`; списание 1 запроса в единственной точке `charge_one` после успеха. `check_another` («Проверить другого»). Заглушка `screen_employer` удалена; навигация без тупиков: «Проверить другого» / «Задать вопрос» (→ ask.enter_ask) / «Главное меню» (→ menu). Проверка против реального агента: ИНН 7707083893 → ПАО Сбербанк + 20 источников; «Пятёрочка» → просит уточнить ИНН. Живая проверка человеком (@Polarasing), все 4 критерия из лога: «ИНН 7718280702» → сводка + 19 источников, баланс 3→2; «Проверить другого» → «ИНН 750400684966» → сводка, 2→1; при balance=0 «Проверить работодателя» → увод в оплату; выходы «Проверить другого»/«Задать вопрос»/«Главное меню» — все работают. Тестовый баланс пополнялся/обнулялся вручную (оплата — Этап 6).

2026-07-03 — Этап 4: Адаптивный сбор ситуации (FSM + flash-lite) — tag stage-4-done — состояние `COLLECTING` перед поиском (`src/handlers/ask.py`, два FSM-состояния `waiting_question`→`collecting`). Служебная модель `ai.collect_decide` на `gemini-2.5-flash-lite` возвращает structured JSON по контракту §4.2 (`response_format: json_object`), с `_normalize_decision` + терпимым `_loads_lenient`. `confidence` привязан в промпте к достаточности данных (иначе flash-lite переоценивал уверенность 0.9 на расплывчатом вопросе и пропускал сбор). Цикл `_collect_step` под капом `max_collect_steps=3`, ранний выход по `enough`/`collect_confidence=0.75`; кнопки «Ответить сейчас» (оборвать сбор) и «Отмена» (в меню); off-topic → редирект без списания; сводка `case` (§4.4 `_build_summary`) → в существующий `answer_with_search`; списание — одна точка `_finalize_answer` (charge_one), `_guard_balance` перед дорогими шагами. **Ключевой баг, найденный на живой проверке**: провайдер OpenRouter под flash-lite ~20% вызовов отдаёт `finish_reason=error` с пустым/битым JSON → парсер падал, срабатывала деградация «сразу к ответу» и уточнение не задавалось (симптом: «не платят зарплату» → сразу поиск). Фикс: `collect_decide` ретраит до `collect_retries=3`, пропуская битые ответы; 10/10 успешных решений в проверке. Config: `max_collect_steps/collect_confidence/collect_max_tokens=1000/collect_retries=3`. Живая проверка человеком (@Polarasing), все 5 критериев из лога: «не платят зарплату» → уточнение шаг 1/3, 4 варианта; «ООО НАШЕКРАШЕ» → 0 уточнений (enough шаг 1); «сломал станок» → уточнение → «Ответить сейчас» → финал; балансы 5→4→3 (одно списание за весь поток сбор+поиск); «какая погода завтра» → off-topic редирект без списания. Тестовый баланс пополнялся вручную (оплата — Этап 6).

2026-07-02 — Этап 3: ИИ-агент с веб-поиском (Tavily + Exa/Firecrawl) — tag stage-3-done — `src/search.py`: единый `run_web_search(query)` с fallback-цепочкой Tavily→Exa→Firecrawl (провайдер участвует только при наличии ключа; первый непустой результат выигрывает; нормализация к `{title,url,snippet}`, клип 500 симв.; логи выбранного провайдера/переключений). `src/ai.py` `answer_with_search`: tool-calling петля на `model_answer` с инструментом `web_search`, кап `max_search_steps`(=4) + токен-бюджет(`search_token_budget`), форс-финал (`tool_choice=none`) при упоре в кап/бюджет; **первый шаг форсирует поиск** (`tool_choice`=web_search) — иначе Gemini ленится и отвечает по устаревшей памяти (грундинг §1.2); возвращает `(текст, источники)`, dedup URL с порядком. `src/handlers/ask.py`: статусы «Ищу информацию…/Проверяю источники…» редактированием одного сообщения (не молчим §5.3), блок «Источники» (`texts.sources_block`), превью ссылок отключено (`LinkPreviewOptions`), списание — одна точка после успеха за весь поиск. Config: `max_search_steps/search_results_per_query/search_token_budget/search_timeout`. Ключи из `.env` (заказчик прислал Tavily/Exa/Firecrawl; `web_search_api-key.txt` → в `.gitignore`; на этапе 7 переедут в `app_settings` с горячей заменой). Verification (в контейнере): все 3 ключа валидны (по 5 рез.); агент «МРОТ 2026» → Tavily → ответ 27 093 ₽ + 5 источников; форс-поиск на шаге 0; fallback Tavily 401 → Exa автоматически. Живая проверка человеком (@Polarasing): «МРОТ 2026» → статус → ответ + 5 источников, баланс 4→3 (одно списание за весь поиск), лог «Поиск Tavily → 5 рез.»/«Агент: финал на шаге 1»/«источников 5». Все 5 критериев сошлись.

2026-07-02 — Этап 2: Базовый ИИ-ответ + память + списание — tag stage-2-done — клиент OpenRouter `src/ai.py` (aiohttp, модель `model_answer`=`gemini-2.5-flash`, системный промпт: русский, простой язык под аудиторию 35–50, строго HTML-теги Telegram без Markdown, осторожная оговорка, без эмодзи; капы `ai_max_tokens`/`ai_request_timeout`; класс `AIError`). Память диалога `src/memory.py` в Redis (ключ `urist:dialog:{tg_id}`, JSON-хвост последних `dialog_memory_messages`, TTL `dialog_ttl_seconds`=3 дня; `get_history`/`append`/`clear`). Списание — единственная точка `repo.charge_one` (атомарный `UPDATE ... WHERE balance>0 RETURNING balance`, None при нуле — защита от гонки и минуса). Ветка `src/handlers/ask.py` (FSM `AskStates.waiting_question`): вход из меню → при `balance=0` уводит в оплату, иначе приглашение; текст-вопрос → статус «готовлю ответ» + typing → `ai.answer(history, q)` → при успехе `charge_one` → `memory.append` → ответ + футер с остатком; `AIError` → сообщение без списания; «Новый диалог» → `memory.clear`; кнопки-ярлыки (`_MENU_LABELS`) в состоянии не уходят в модель, «Главное меню» проваливается в menu-роутер. `open_ask`-заглушка убрана из menu.py; ask-роутер включён до menu. Config: `ai_base_url/timeout/max_tokens/temperature`, `dialog_memory_messages/ttl`. Verification (в контейнере): реальный ответ OpenRouter (HTML, RU); память 4 msg верный порядок; `charge_one` 2→1→0→None. Живая проверка человеком (@Polarasing): 4 ответа ИИ, уточнение по контексту «А если откажут?» (память), «Новый диалог» ×2, «Главное меню» ×5 без ухода в ИИ, баланс убывает. Все 5 критериев сошлись.

2026-07-02 — Этап 1: Онбординг + главное меню + БД юзера — tag stage-1-done — `repo.upsert_user` теперь возвращает `(is_new, balance)` и начисляет `free_requests_on_start` (дефолт 3) новичку на INSERT; детект новой строки по системному `xmax=0`; повторный `/start` обновляет только профиль/last_active, баланс не трогает; добавлен `get_balance`. Тексты в `texts.py` (приветствие нов./возврат, юр-дисклеймер, 4 экрана, «Помощь» с дисклеймером и контактом поддержки-заглушкой `@URIST2026_support`) — HTML, без эмодзи. Reply-клавиатуры в `keyboards.py` (главное меню 4 ветки + навигация экранов «Главное меню»). `handlers/start` — онбординг нового vs возврат. `handlers/menu` — 4 ветки на экраны-заглушки без тупиков + фолбэк на любой нераспознанный текст. Verification: SQL-проба upsert (новичок→is_new=t,balance=3; после списания и повторного /start→is_new=f,balance=2). Живая проверка человеком: сброшена запись @Polarasing → `/start` создал новичка с balance=3, прошёл по кнопкам меню (события в user_events: «Задать вопрос»/«Помощь»/«Главное меню»), повторный /start баланс не удвоил. Все 5 критериев сошлись. Открытый вопрос: заменить заглушку контакта поддержки на реальный.

2026-07-02 — Этап 0: Каркас репозитория — tag stage-0-done — миграция `0001_init` (схема `urist_bot` + таблицы `users` с полями balance/is_paying, `fsm_states`, `user_events`). Каркас `src/` по образцу club_bot: config (pydantic-settings, валидация имени схемы), logger (Loguru, русский формат), db (asyncpg pool, search_path на свою схему, max_size=20 под ориентир 500 юзеров), cache (Redis), migrate (Alembic auto-upgrade при старте до event loop), middlewares (LoggingMiddleware + запись в user_events), main (aiogram 3, RedisStorage FSM, HTML parse_mode, polling). Docker/Compose dev с hot-reload через watchfiles. Нюанс: `docker compose` требует явный `-p urist_bot` — имя папки кириллическое («юр.агент»), автоимя проекта выходит пустым. Ещё: убрал явный пин `aiohttp` — конфликтовал с `aiogram 3.15` (требует aiohttp<3.11). Verification: `@Polarasing` (id=2042819654, Kira) отправил /start → получил ответ-заглушку; лог `👤 @Polarasing (id:2042819654, Kira) → /start`; в `urist_bot.users` создалась запись (balance=0); в `user_events` — «Команда /start». Схема и таблицы подтверждены `\dn`/`\dt`. Hot-reload: touch src → «1 change detected» → рестарт. Все 5 критериев сошлись.

## Протокол обновления

**При старте сессии**:
1. Прочитать этот файл.
2. `git tag -l "stage-*"` и сверить с колонкой «Закрывающий тег». Тег без записи в таблице = таблица устарела, обновить.
3. `git log --oneline -10` — что менялось последним.
4. Только после этого приступать к работе.

**При взятии этапа в работу**:
1. Обновить «Текущий этап» и «Активная работа».
2. Статус 🚧 в таблице.
3. Закоммитить отдельно: `chore(status): start stage N`.

**При закрытии этапа**:
1. Все чекбоксы в `07_ROADMAP.md` отмечены `[x]` и реально проверены.
2. Тег: `git tag stage-N-done -m "<описание>"`.
3. В таблице: ✅, тег, коммит (`git rev-parse --short HEAD`), дата.
4. Запись в «Историю закрытий».
5. Новый «Текущий этап» + «Следующий шаг».
6. Закоммитить: `chore(status): close stage N`.

**Если этап заблокирован**:
1. Статус ⏸ в таблице.
2. В «Известные блокеры»: что блокирует, кого ждём, что нужно для разблокировки.

## Что НЕ хранится здесь

- Длинные размышления и планы — `07_ROADMAP.md` и спеки в `docs/`.
- TODO внутри текущего этапа — таск-лист активной сессии.
- История изменений кода — `git log`.
