# STATUS — текущее состояние проекта (Юр-бот «URIST2026»)

> **Для Claude и человека**: это первый файл, который читается в новой сессии. После
> прочтения **обязательно** сверить с реальностью: `git tag -l "stage-*"`,
> `git log --oneline -20` — таблица ниже может быть устаревшей.

**Последнее обновление**: 2026-07-02 (Этап 1 взят в работу)
**Текущий этап**: Этап 1 — Онбординг + меню + БД юзера (🚧 в работе)
**Следующий шаг**: Реализовать приветствие + дисклеймер + начисление N бесплатных запросов на первый `/start`, главное меню reply-клавиатурой (4 ветки на экраны-заглушки, без тупиков). Критерии — в [`07_ROADMAP.md`](07_ROADMAP.md#этап-1--онбординг--главное-меню--бд-пользователя).

---

## Сводка этапов

Легенда: ☐ не начат · 🚧 в работе · ✅ закрыт · ⏸ заблокирован

| # | Этап | Статус | Закрывающий тег | Последний коммит | Дата закрытия |
|---|---|---|---|---|---|
| 0 | Каркас репозитория | ✅ | `stage-0-done` | `80506b3` | 2026-07-02 |
| 1 | Онбординг + меню + БД юзера | 🚧 | `stage-1-done` | — | — |
| 2 | Базовый ИИ-ответ + память + списание | ☐ | `stage-2-done` | — | — |
| 3 | ИИ-агент с веб-поиском (Tavily) | ☐ | `stage-3-done` | — | — |
| 4 | Адаптивный сбор ситуации (FSM) | ☐ | `stage-4-done` | — | — |
| 5 | «Проверить работодателя» | ☐ | `stage-5-done` | — | — |
| 6 | Оплата ЮKassa (пакеты 10/20/30) | ☐ | `stage-6-done` | — | — |
| 7 | Админка `/admin` | ☐ | `stage-7-done` | — | — |
| 8 | Помощь + дисклеймеры + анти-абьюз | ☐ | `stage-8-done` | — | — |
| 9 | Аналитический слой БД | ☐ | `stage-9-done` | — | — |
| 10 | Прод: деплой + CI/CD | ☐ | `stage-10-done` | — | — |

Детальные критерии приёмки каждого этапа — в [`07_ROADMAP.md`](07_ROADMAP.md).

## Активная работа

Этап 1 — онбординг + главное меню. Наращиваем поверх каркаса: `repo.upsert_user`
возвращает `(is_new, balance)` и начисляет `free_requests_on_start` новичку; тексты
(приветствие/дисклеймер/экраны) в `texts.py`, reply-клавиатуры в `keyboards.py`;
`/start` показывает приветствие+дисклеймер новичку и главное меню всем; 4 ветки меню
ведут на экраны-заглушки с выходом «Главное меню» (без тупиков).

## Известные блокеры

Нет.

## История закрытий

2026-07-02 — Этап 0: Каркас репозитория — tag stage-0-done — миграция `0001_init` (схема `urist_bot` + таблицы `users` с полями balance/is_paying, `fsm_states`, `user_events`). Каркас `src/` по образцу club_bot: config (pydantic-settings, валидация имени схемы), logger (Loguru, русский формат), db (asyncpg pool, search_path на свою схему, max_size=20 под ориентир 500 юзеров), cache (Redis), migrate (Alembic auto-upgrade при старте до event loop), middlewares (LoggingMiddleware + запись в user_events), main (aiogram 3, RedisStorage FSM, HTML parse_mode, polling). Docker/Compose dev с hot-reload через watchfiles. Нюанс: `docker compose` требует явный `-p urist_bot` — имя папки кириллическое («юр.агент»), автоимя проекта выходит пустым. Ещё: убрал явный пин `aiohttp` — конфликтовал с `aiogram 3.15` (требует aiohttp<3.11). Verification: `@Polarasing` (id=2042819654, Kira) отправил /start → получил ответ-заглушку; лог `👤 @Polarasing (id:2042819654, Kira) → /start`; в `urist_bot.users` создалась запись (balance=0); в `user_events` — «Команда /start». Схема и таблицы подтверждены `\dn`/`\dt`. Hot-reload: touch src → «1 change detected» → рестарт. Все 5 критериев сошлись.

## Протокол обновления

**При старте сессии**:
1. Прочитать этот файл.
2. `git tag -l "stage-*"` и сверить с колонкой «Закрывающий тег». Тег без записи в таблице = таблица устарела, обновить.
3. `git log --oneline -10` — что менялось последним.
4. Только после этого приступать к работе.

**При взятии этапа в работу**:
1. Обновить «Текущий этап» и «Активная работа».
2. Статус 🚧 в таблице.
3. Закоммитить отдельно: `chore(status): start stage N`.

**При закрытии этапа**:
1. Все чекбоксы в `07_ROADMAP.md` отмечены `[x]` и реально проверены.
2. Тег: `git tag stage-N-done -m "<описание>"`.
3. В таблице: ✅, тег, коммит (`git rev-parse --short HEAD`), дата.
4. Запись в «Историю закрытий».
5. Новый «Текущий этап» + «Следующий шаг».
6. Закоммитить: `chore(status): close stage N`.

**Если этап заблокирован**:
1. Статус ⏸ в таблице.
2. В «Известные блокеры»: что блокирует, кого ждём, что нужно для разблокировки.

## Что НЕ хранится здесь

- Длинные размышления и планы — `07_ROADMAP.md` и спеки в `docs/`.
- TODO внутри текущего этапа — таск-лист активной сессии.
- История изменений кода — `git log`.
